version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:8000"
    environment:
      - PORT=8000
      - SECRET_KEY=your-secret-key-here
      - FLASK_DEBUG=False
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    restart: unless-stopped
